{
  "name": "drastic",
  "packageManager": "bun@1.2.8",
  "private": true,
  "scripts": {
    "dev": "bun --cwd apps/web dev",
    "build": "bun --cwd apps/web build",
    "ui": "bunx --bun shadcn@latest",
    "clean": "rm -rf node_modules && rm -rf apps/*/node_modules && bun i"
  },
  "workspaces": [
    "apps/*"
  ]
}
